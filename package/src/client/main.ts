/**
 * Client-side entry point for js-mvc.
 * Compiled to public/.generated/client/main.js and loaded by the
 * server-rendered layout; also imported by the inline hydration scripts
 * that ship with each rendered handler component.
 */

export { hydrate, register } from "./dispatcher";
